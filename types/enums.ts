export enum WidgetType {
	Static = 'Static',
	Dynamic = 'Dynamic',
}

export enum SelectionTypes {
	FixedCard = 'Fixed Card',
	Carousel = 'Carousel',
}

export enum TileTypes {
	Web = 'Web',
	Mobile = 'Mobile',
}